name: Build KernelSU Kernel Module

on:
  workflow_call:
    inputs:
      kmi:
        description: 'KMI version'
        required: true
        type: string
      ddk_release:
        description: 'DDK release version'
        required: false
        default: '20260313'
        type: string
      expected_size2:
        description: 'Second expected signature size (for PR builds)'
        required: false
        default: ''
        type: string
      expected_hash2:
        description: 'Second expected signature hash (for PR builds)'
        required: false
        default: ''
        type: string

jobs:
  build-kernelsu-ko:
    name: Build kernelsu.ko for ${{ inputs.kmi }}
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/ylarod/ddk-min:${{ inputs.kmi }}-${{ inputs.ddk_release }}
      options: --privileged

    steps:
    # --- [新增] 第一步：自动去官方仓库查询最新的 Release 版本号 ---
    - name: Get latest KernelSU release tag
      id: get_latest_tag
      run: |
        # 用 curl 调用 GitHub API 获取最新发布版，并用 jq 提取出 tag_name (如 v0.9.5)
        LATEST_TAG=$(curl -sL https://api.github.com/repos/tiann/KernelSU/releases/latest | jq -r '.tag_name')
        echo "🔥 Found latest official release tag: $LATEST_TAG"
        # 将获取到的版本号存入输出变量中，供下一步使用
        echo "tag=$LATEST_TAG" >> $GITHUB_OUTPUT

    # --- [修改] 第二步：根据上一步获取的版本号，精准拉取官方纯净源码 ---
    - name: Checkout Official Stable Release
      uses: actions/checkout@v6
      with:
        repository: tiann/KernelSU
        ref: ${{ steps.get_latest_tag.outputs.tag }} # 动态使用刚才获取到的最新标签！
        fetch-depth: 0
    # -----------------------------------------------------------------

    # --- 原版 KernelSU 专属暗杀者注入补丁 ---
    - name: Inject Oplus Guard Sniper Patch
      run: |
        cat << 'EOF' > patch_ksu.py
        import os

        target_file = "kernel/core/init.c"
        if not os.path.exists(target_file):
            print(f"❌ Error: {target_file} not found!")
            exit(1)

        with open(target_file, "r") as f:
            content = f.read()

        injector_funcs = """
        #include <linux/notifier.h>
        static int guard_tripwire_callback(struct notifier_block *nb, unsigned long action, void *data) {
            struct module *mod = data;
            if (action == MODULE_STATE_COMING && mod) {
                if (strcmp(mod->name, "oplus_secure_guard_new") == 0) {
                    pr_alert("KernelSU Sniper: Tripwire triggered! Blocking %s natively.\\n", mod->name);
                    return notifier_from_errno(-EPERM);
                }
            }
            return NOTIFY_DONE;
        }
        static struct notifier_block guard_tripwire_nb = { .notifier_call = guard_tripwire_callback, .priority = INT_MAX };
        static void clean_preexisting_guard(void) {
            char *envp[] = { "HOME=/", "TERM=linux", "PATH=/sbin:/system/sbin:/system/bin:/vendor/bin", NULL };
            char *argv[] = { "/system/bin/rmmod", "oplus_secure_guard_new", NULL };
            call_usermodehelper(argv[0], argv, envp, UMH_WAIT_PROC);
        }

        int __init kernelsu_init(void)
        """

        # 新的锚点替换逻辑：插在 prepare_creds 之前
        injector_calls = """    register_module_notifier(&guard_tripwire_nb);
            clean_preexisting_guard();

            ksu_cred = prepare_creds();"""

        if "guard_tripwire_callback" not in content:
            # 1. 注入函数体
            content = content.replace("int __init kernelsu_init(void)", injector_funcs)
            # 2. 注入激活命令 (原版适配)
            content = content.replace("ksu_cred = prepare_creds();", injector_calls)
            with open(target_file, "w") as f:
                f.write(content)
            print("✅ Oplus Sniper patch applied successfully for Official KSU!")
        else:
            print("⚠️ Patch already applied, skipping.")
        EOF
        python3 patch_ksu.py
    # --- 注入补丁结束 ---

    - name: Build kernelsu.ko
      env:
        EXPECTED_SIZE2: ${{ inputs.expected_size2 }}
        EXPECTED_HASH2: ${{ inputs.expected_hash2 }}
      run: |
        git config --global --add safe.directory /__w/KernelSU/KernelSU

        cd kernel

        echo "=== Building kernelsu.ko for KMI: ${{ inputs.kmi }} ==="

        EXTRA_ARGS=""
        if [ -n "$EXPECTED_SIZE2" ] && [ -z "$EXPECTED_HASH2" ]; then
          echo "ERROR: expected_hash2 must be provided when expected_size2 is set."
          exit 1
        fi
        if [ -z "$EXPECTED_SIZE2" ] && [ -n "$EXPECTED_HASH2" ]; then
          echo "ERROR: expected_size2 must be provided when expected_hash2 is set."
          exit 1
        fi
        if [ -n "$EXPECTED_SIZE2" ]; then
          EXTRA_ARGS="KSU_EXPECTED_SIZE2=$EXPECTED_SIZE2 KSU_EXPECTED_HASH2=$EXPECTED_HASH2"
        fi
        CONFIG_KSU=m CC=clang make $EXTRA_ARGS
        
        echo "=== Build completed ==="

        # Create output directory in GitHub workspace
        mkdir -p /github/workspace/out

        # Copy with KMI-specific naming
        OUTPUT_NAME="${{ inputs.kmi }}_kernelsu.ko"
        cp kernelsu.ko "/github/workspace/out/$OUTPUT_NAME"
        
        echo "Copied to: /github/workspace/out/$OUTPUT_NAME"
        ls -la "/github/workspace/out/$OUTPUT_NAME"
        echo "Size: $(du -h "/github/workspace/out/$OUTPUT_NAME" | cut -f1)"
        llvm-strip -d "/github/workspace/out/$OUTPUT_NAME"
        echo "Size after stripping: $(du -h "/github/workspace/out/$OUTPUT_NAME" | cut -f1)"

    - name: Upload kernelsu.ko artifact
      uses: actions/upload-artifact@v7
      with:
        name: ${{ inputs.kmi }}-lkm
        path: /github/workspace/out/${{ inputs.kmi }}_kernelsu.ko
